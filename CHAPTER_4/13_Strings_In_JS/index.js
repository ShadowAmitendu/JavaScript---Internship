// Strings in JS

// printing Strs in different ways!

let str = "HELLO"; //Double Quotes
let STR = "hello"; //Single Quotes
let sTr = `HellO`; //Template Literal

console.log(str, STR, sTr);
console.log(str[0], STR[0], sTr[0]);
console.log(str[4], STR[4], sTr[4]);

// ESCAPE SEQUENCE are those characters that have a backslash before them like \n = newline, \t = tab etc.
