// Introduction to Arrays

// Arrays are Mutable that means the content can be changed of any nth position in the array.

let arr = [1, 2, 3, 4, 5, 6];

console.log(arr[0]);
console.log(arr);

for (let index = 0; index < arr.length; index++) {
	console.log(arr[index]);
}
